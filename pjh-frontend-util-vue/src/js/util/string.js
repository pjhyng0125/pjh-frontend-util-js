/**
 * 문자열 유틸리티 js
 */
export const stringUtil = {
  /**
   * 문자열을 구분자로 쪼개기
   * @param {string} s
   * @param {string} d
   * @returns {array}
   */
  splitByDelimiter: (s, d) => {
    return s.split(d);
  },
};
