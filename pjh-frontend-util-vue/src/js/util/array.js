/**
 * 배열 유틸리티 js
 */
export const arrayUtil = {
  /**
   * 배열 합치기
   * @param {array} source
   * @param {array} addTo
   * @returns {array}
   */
  mergeArr: (source, addTo) => {
    return source.concat(addTo);
  },
};
